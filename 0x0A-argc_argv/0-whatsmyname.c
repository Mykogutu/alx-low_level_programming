#include <stdio.h>

/**
 * main - prints the nam of the program
 * @argc: number of arguments
 * @argv: array of CLI arguments passed
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
